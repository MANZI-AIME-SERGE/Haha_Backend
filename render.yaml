{
  "name": "haha-supermarket-backend",
  "plan": "free",
  "buildCommand": "npm install && npm run seed",
  "startCommand": "npm start",
  "envVars": {
    "NODE_ENV": {
      "value": "production"
    },
    "JWT_SECRET": {
      "value": "change-this-to-a-secure-random-string"
    },
    "MONGODB_URI": {
      "value": "",
      "description": "Your MongoDB connection string from MongoDB Atlas"
    }
  }
}
